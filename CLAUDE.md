# Meeting Slot Picker

## セッション跨ぎの記憶（最初に読む）

このプロジェクトの「文脈・背景・進行状態」は @docs/PROJECT_STATE.md に集約している。
**新しいセッションでは、まず PROJECT_STATE.md の内容を前提として作業する。**
区切りのよい作業完了時・方針決定時・ブランチ切替時、またはユーザーが「状態保存」と言ったら、
その時点の最新状態で `docs/PROJECT_STATE.md` を更新すること。

