# Google OAuth クライアント (種別: Desktop app) の client ID
# GCP コンソールで Calendar API を有効化し、OAuth 同意画面に scope calendar.readonly を設定すること。
VITE_GOOGLE_CLIENT_ID=

# 一部の Desktop クライアントは token endpoint で client_secret を要求する。
# 必要な場合のみ設定 (Desktop クライアントの secret は機密ではない扱い)。
VITE_GOOGLE_CLIENT_SECRET=
